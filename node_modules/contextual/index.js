//Contextual Decision Making Engine
exports.geo = function (context) {
	console.log('Contextual Engine: Processing ' + context)
	var geo = context
	return context
}

exports.want = function (context) {
	console.log('Context Engine: Processing ' + context)
	var want = context
	return context
}